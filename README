This docker container uses https://github.com/stecklars/dynamic-dns-netcup-api

For Usage:
- create a config.php within build dir (see https://github.com/stecklars/dynamic-dns-netcup-api/blob/master/config.dist.php)
- fill with your credentials
- build container
- spin up container

DNS will be check/updated every 15 minutes